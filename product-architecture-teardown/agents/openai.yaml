interface:
  display_name: "产品架构拆解"
  short_description: "基于可核验证据，按产品类型完成用户、技术、模型和数据四层拆解"
  default_prompt: "Use $product-architecture-teardown to classify this product, verify the evidence is sufficient, and produce a traceable four-layer HTML teardown."
policy:
  allow_implicit_invocation: true
